echo:
  auth:
    basic:
      enabled: true
      users:
        - username: admin
          password: "{noop}q1w2e3r4"
          roles:
            - ADMIN
            - USER

  organization:
    name: dupasa
    definition-path: ./units.yaml

  #TODO disable devtools on prod

management:
  server:
    port: 8081
  endpoint:
    health:
      enabled: true

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:file:./echo;DB_CLOSE_ON_EXIT=FALSE
  ldap:
    urls: ldap://172.19.121.15:389
